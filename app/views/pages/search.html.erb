<!DOCTYPE html>
<html lang="en">
  <head>
      <%= stylesheet_link_tag 'collection_pages', media: 'all', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <h3>Search Result</h3>  
    <div class="items-container"> 
      <% @results.each do |result| %>  
        <div class="images-for-collection">
          <%= link_to image_tag(result.image_url,  width: '150px', height: '180px', class: 'image-fit'), 
                                              saved_items_path(:product_id => result.id), method: :post %>
          <%= link_to  saved_items_path(:product_id => result.id), method: :post do %>
                <p><b><%= result.name %></b> <br> <%= number_to_currency(result.price) %></p>
            <% end %>
        </div>   
      <% end %>
    </div>
  </body>

</html>






